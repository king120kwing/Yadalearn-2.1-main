# Rules

- Do not push anything to the git repository unless the user explicitly gives the okay sign or the green light to do so. This is a strict rule that must be followed when building this project.
